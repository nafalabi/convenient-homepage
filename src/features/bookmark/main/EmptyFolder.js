import { Typography } from "@mui/material";
import React from "react";

const EmptyFolder = () => {
  return (
    <div>
      <Typography color="textSecondary">No data in this folder</Typography>
    </div>
  );
};

export default EmptyFolder;
