export enum ImageProvider {
  UNSPLASH = 0,
  PIXABAY = 1,
  BING = 2,
}

export enum IconType {
  EMOJI = 0,
  MATERIAL_ICON = 1,
  URL = 2,
}

export type TimeCycleUnits = "weeks" | "days" | "hours" | "minutes";
